<h1><%= @category.title %></h1>
<% @posts.each do |post| %>
  <p><%= link_to(post.title, category_post_path(@category, post)) %></p>
<% end %>

<%= link_to("Create Post", new_category_post_path(@category)) %>
